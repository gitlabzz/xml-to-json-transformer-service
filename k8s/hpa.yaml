apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: xml-json
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: xml-json
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
